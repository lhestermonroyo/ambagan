import AUTH_STATE from './auth.state';
import GROUP_STATE from './group.state';
import TRANSACTION_STATE from './transaction.state';

const states = {
  auth: AUTH_STATE,
  group: GROUP_STATE,
  transaction: TRANSACTION_STATE
};

export default states;
