// Feature flags for gating in-progress features.
// Flip to true to re-enable in a future release.

// "Add from Contacts" import — deferred past the 1.2 release in favor of
// QR-code group invites. Component and expo-contacts wiring remain in place.
export const CONTACTS_IMPORT_ENABLED = false;
